export default (MODE: string) => {
  const POST = {
    v1: {
      generalRoutes: [],
    },
  };

  const GET = {
    v1: {
      generalRoutes: [],
    },
  };

  return {
    POST,
    GET,
  };
};
